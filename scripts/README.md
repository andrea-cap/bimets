# Development scripts

These tools support repository maintenance and are not part of the installed
`bimets` package.

- `generate_api_reference.py` rebuilds the static API pages from public
  docstrings.
